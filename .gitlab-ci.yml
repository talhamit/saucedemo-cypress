stages:
  - test

cypress_job:
  image: cypress/browsers:node18.12.0-chrome107
  stage: test
  script:
    - node -v
    - npm -v
    - npm ci
    - npx cypress run --browser chrome --headless
  artifacts:
    when: always
    paths:
      - cypress/screenshots/
      - cypress/videos/
